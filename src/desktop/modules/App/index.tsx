import { Button } from '@chakra-ui/react';
import React from 'react';

function App() {
  return (
    <div>
      App inside
      {/* <div>
        <Button onClick={() => navigate('/')} />
      </div> */}
    </div>
  );
}

export default App;
