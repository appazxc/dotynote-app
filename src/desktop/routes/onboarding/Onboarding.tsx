import { OnboardingContent } from 'shared/modules/onboarding/OnboardingContent';

import { Layout } from 'desktop/components/Layout';

function Onboarding() {
  return (
    <Layout>
      <OnboardingContent />
    </Layout>
  );
}

export { Onboarding };

export default Onboarding;
