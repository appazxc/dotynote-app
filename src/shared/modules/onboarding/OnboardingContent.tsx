import { Box, Text } from '@chakra-ui/react';
import React from 'react';

type Props = {};

export const OnboardingContent = React.memo((props: Props) => {
  return (
    <Box>
      <Text>Onboarding</Text>
    </Box>
  );
});
