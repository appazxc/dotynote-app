import { Box } from '@chakra-ui/react';
import React from 'react';

type Props = {};

export const BillingContent = React.memo((props: Props) => {
  return (
    <Box>
      billing
    </Box>
  );
});
